enum WorkspaceEndPoint {
  base = "/workspace/",

  // getAll = "/workspace/",
  // getById = "/workspace/${id}/",
  // create = "/workspace/",
  // update = "/workspace/${id}/",
  // remove = "/workspace/${id}/",
}

export const EndPoints = {
  workspace: WorkspaceEndPoint,
};
